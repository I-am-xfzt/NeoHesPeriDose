{
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  },
  "scripts": {
    "start:server": "cd koa-file-explorer && npm start",
    "start:web": "cd web-ui && pnpm dev",
    "dev": "npm-run-all --parallel start:server start:web"
  }
}
